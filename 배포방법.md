# 배포

Windows 서버에 Mechanical Design 을 설치하고 갱신하는 절차.

서버가 받는 것은 **릴리스 자산 `deploy_package.zip` 하나**다. 그 안에 코드,
빌드된 화면(SPA), 파이썬 의존성(wheel), 배포 스크립트, 이 문서가 모두 들어 있다.

- 서버에 **git·인터넷이 필요 없다.** 프론트 빌드와 의존성 수집은
  GitHub Actions 가 미리 끝내 둔다.
- **Node.js 는 선택이다.** 앱 구동에는 안 쓴다. 카드 정의 검증
  (`POST /api/cards/<id>/validate`)이 값을 넣어 실제로 계산해 보는데, 그
  계산기가 화면과 같은 자바스크립트 파일이라 Node 가 있어야 돈다. 없으면
  검증이 정적 검사까지만 돌고, 그 사실이 응답의 `trial_skipped` 에 남는다 —
  **검사가 안 돌았는데 통과로 보이지는 않는다.** 밖에서 API 로 카드를
  만들 계획이면(예: MCP·AI) 설치해 두는 편이 낫다.
- 배포는 **검사를 모두 통과한 뒤에야** 폴더를 원자적으로 바꾼다. 중간에
  실패하면 운영 폴더는 손대지 않은 상태 그대로다.
- 직전 버전이 `<AppPath>_prev` 에 남아 있어 `rollback.ps1` 한 줄로 되돌린다.

| 구성 | 값 |
|---|---|
| 앱 포트 | 5176 (`FLASK_PORT`) |
| DB | PostgreSQL 16+ (기본 DB명 `mechanicaldesign`) |
| Python | **3.13** — wheel 의 ABI 태그가 이 버전에 묶여 있다 |
| 서버 | Waitress (`FLASK_ENV=production`), NSSM 으로 Windows 서비스 등록 |

접속은 `http://서버IP:5176` 하나로 한다. 백엔드 한 프로세스가 API 와 화면을
모두 서빙하므로 프론트용 포트가 따로 없다.

---

## 0. 사람이 미리 준비할 것

서버에:

1. **Python 3.13** — 설치할 때 "Add python.exe to PATH" 를 켠다.
   `py -0p` 로 3.13 이 목록에 보이면 된다.
2. **PostgreSQL 16+** — 서비스가 돌고 있어야 하고 `postgres` 비밀번호를 안다.
   같은 서버에 둘 필요는 없다. 원격이면 `-DbHost` 로 지정한다.
3. 앱을 둘 폴더를 정한다. 이 문서는 `C:\Server\MechanicalDesign` 을 쓴다.
4. *(선택)* **Node.js 20+** — 카드 정의 검증의 시험 계산에만 쓴다.
   밖에서 API 로 카드를 만들 계획이 없으면 안 깔아도 된다. `precheck.ps1` 이
   있는지 없는지를 알려 주고, 없어도 설치를 막지는 않는다.

배포하는 PC 에 (폐쇄망이 아니라면 서버에서 해도 된다):

5. **gh CLI** + `gh auth login` — 저장소가 비공개라 릴리스를 받으려면 필요하다.
   폐쇄망이면 다른 PC 에서 zip 을 받아 반입하고 `-ZipPath` 를 쓴다.

---

## 1. 초기 배포 (Day 0)

### 1-1. 배포 스크립트 꺼내기

스크립트는 zip 안에 들어 있다. zip 을 받아 한 번만 손으로 펼치면 그다음부터는
그 스크립트들이 배포를 처리한다.

```powershell
# 인터넷이 되는 경우
mkdir C:\Server\tools\MechanicalDesign
cd C:\Server\tools\MechanicalDesign
gh release download --repo 4leaf321-nox321/MechanicalDesign `
  --pattern deploy_package.zip --dir .
Expand-Archive .\deploy_package.zip -DestinationPath .\pkg -Force
Copy-Item .\pkg\*.ps1 . -Force
Copy-Item .\pkg\배포방법.md . -Force
```

> 스크립트는 `C:\Server\tools\MechanicalDesign\` 처럼 **프로젝트별 하위 폴더**에
> 둔다. `C:\Server\tools` 바로 아래에 두면 같은 서버의 다른 앱과 파일명이 겹쳐
> 서로를 덮어쓴다.

### 1-2. 환경 점검

설치를 시작하기 전에 막을 수 있는 것을 먼저 막는다. Python 버전, 디스크 여유,
쓰기 권한, 포트 5176, PostgreSQL 도달·인증을 본다.

```powershell
.\precheck.ps1 -AppPath 'C:\Server\MechanicalDesign' `
  -DatabaseUrl 'postgresql+psycopg://postgres:비밀번호@localhost:5432/mechanicaldesign'
```

문제가 있으면 목록으로 찍고 멈춘다. 전부 해결한 뒤 다음으로 넘어간다.

### 1-3. 설치

관리자 PowerShell 에서 실행한다 — 방화벽 규칙과 서비스 등록에 필요하다.

```powershell
.\install.ps1 -AppPath 'C:\Server\MechanicalDesign' -DbPassword 'PostgreSQL비밀번호'

# 폐쇄망이면 반입한 zip 을 직접 지정
.\install.ps1 -AppPath 'C:\Server\MechanicalDesign' `
  -ZipPath 'C:\tmp\deploy_package.zip' -DbPassword 'PostgreSQL비밀번호'
```

하는 일:

1. 환경 점검 (`-SkipPrecheck` 로 생략 가능)
2. 코드 배치 — `deploy.ps1` 을 그대로 호출한다. 설치와 갱신이 같은 경로를 타야
   "설치는 되는데 갱신이 안 되는" 상태가 생기지 않는다.
3. `<AppPath>_data\uploads`, `<AppPath>_data\logs` 생성
4. `backend\.env` 생성 — `SECRET_KEY`·`JWT_SECRET_KEY` 는 난수로 만든다.
   **이미 있으면 건드리지 않는다.**
5. 데이터베이스 생성 (없을 때만)
6. `flask db upgrade`
7. **초기 관리자 계정** — 기본값은 아이디 `admin`, 비밀번호 `32167` 이다.
   이미 있으면 비밀번호를 바꾸지 않는다.
8. 방화벽 TCP 5176 열기
9. NSSM 으로 Windows 서비스 `MechanicalDesign` 등록 + 시작
   (`-NoService` 로 생략하면 콘솔 실행이 된다)

> **비밀번호를 난수로 받고 싶으면** `-AdminPassword ''` 를 주세요. 그러면 설치가
> 난수를 만들어 **화면에 한 번만** 출력하고 첫 로그인에서 변경을 강제합니다.
> 어디에도 저장하지 않으므로 그 출력을 놓치면 다시 볼 수 없습니다.

자주 쓰는 옵션:

| 옵션 | 뜻 |
|---|---|
| `-DbHost` / `-DbPort` / `-DbName` / `-DbUser` | DB 접속 정보 (기본 localhost:5432 / mechanicaldesign / postgres) |
| `-Port` | 앱 포트 (기본 5176) |
| `-Tag v0.2.0` | 특정 버전을 설치 |
| `-PythonExe` | 쓸 python.exe 를 직접 지정 |
| `-ServiceName` | 서비스 이름 (기본 MechanicalDesign) |
| `-AdminEmail` | 초기 관리자 아이디 (기본 `admin`) |
| `-AdminPassword` | 초기 관리자 비밀번호 (기본 `32167`). 빈 값 `''` 을 주면 난수 + 변경 강제 |

### 1-4. 확인

```powershell
Get-Service MechanicalDesign
Invoke-WebRequest http://localhost:5176/api/health -UseBasicParsing
```

`{"status":"ok"}` 가 나오고 브라우저에서 `http://서버IP:5176` 에 **로그인 화면**이
뜨면 성공이다. 설치가 출력한 관리자 아이디·비밀번호(기본 `admin` / `32167`)로
로그인하면 메인 화면이 보인다.

화면이 안 뜨고 JSON 404 만 나온다면 `frontend\dist` 가 비어 있는 것이다 —
패키지가 잘못 만들어졌다는 뜻이므로 릴리스를 다시 만든다.

---

## 2. 업데이트 배포

### 2-1. 백업 먼저

**되돌릴 수 없는 것은 데이터베이스뿐이다.** 파일은 `_prev` 로 되돌아가지만
마이그레이션은 되돌아가지 않는다.

```powershell
.\backup.ps1 -AppPath 'C:\Server\MechanicalDesign' -BackupRoot 'D:\backup\mechanicaldesign'
```
> **받아만 두지 말고 한 번은 되살려 보세요.** `restore.ps1` 의 기본 동작이
> 리허설이라 운영을 건드리지 않고 확인할 수 있습니다 — 9장.

DB 덤프와 업로드 이미지를 **같은 시각에** 받는다. 한쪽만 받으면 "DB에는 있는데
파일이 없는" 이미지가 생겨 복구가 성립하지 않는다.

### 2-2. 배포 스크립트 갱신

스크립트도 릴리스와 함께 바뀐다. 새 zip 의 것을 쓴다 (1-1 과 같은 방법).

### 2-3. 배포

```powershell
.\deploy.ps1 -AppPath 'C:\Server\MechanicalDesign'

# 특정 버전
.\deploy.ps1 -AppPath 'C:\Server\MechanicalDesign' -Tag v0.2.0

# 폐쇄망
.\deploy.ps1 -AppPath 'C:\Server\MechanicalDesign' -ZipPath 'C:\tmp\deploy_package.zip'
```

하는 일 (순서가 중요하다):

1. 서비스가 돌고 있으면 멈춘다 — 윈도우는 실행 중인 파일을 잠근다.
2. 폴더를 옮길 수 있는지 **실제로 이름을 바꿔 보며** 확인한다. 못 옮기면
   무엇이 잡고 있는지 프로세스·서비스 이름을 대 주고 멈춘다.
   **이 시점까지 운영 폴더는 아무것도 바뀌지 않았다.**
3. zip 을 `<AppPath>_staging` 에 펼치고 검사한다 — wheel 번들, `frontend\dist`,
   그리고 **패키지의 파이썬 버전이 서버 파이썬과 같은지**. 하나라도 어긋나면
   staging 만 지우고 멈춘다.
4. 현재 설치를 `_prev` 로, staging 을 운영 경로로 옮긴다 (원자적 이름 변경).
5. `.env` 를 `_prev` 에서 이어받는다.
6. `requirements.txt` 가 바뀌었을 때만 가상환경을 다시 만든다.
7. `flask db upgrade`
8. 멈췄던 서비스를 다시 켜고 상태를 확인한다.

원래 꺼져 있던 서비스는 켜지 않는다. 일부러 내려 둔 서버가 배포 후에 살아나는
일을 막기 위해서다.

### 2-4. 문제가 생기면

```powershell
.\rollback.ps1 -AppPath 'C:\Server\MechanicalDesign'
```

`_prev` 와 현재 설치를 맞바꾸고, 되돌린 버전의 의존성이 다르면 가상환경도 다시
맞춘다. 서비스가 돌고 있었다면 다시 켠다.

**마이그레이션은 되돌아가지 않는다.** 스키마가 문제라면 `backup.ps1` 로 받아 둔
덤프에서 복구해야 한다 — 절차는 백업 폴더의 `MANIFEST.txt` 에 함께 적혀 있다.

---

## 3. 폴더 배치

```
C:\Server\MechanicalDesign            코드. 배포마다 통째로 교체된다
├─ backend\                           run.py, app\, migrations\, packages\(wheel)
├─ mcp_server\                        MCP 서버 (선택 기능, 8장)
├─ frontend\dist\                     빌드된 화면
├─ BUILD_INFO.txt                     python= / version=
└─ *.ps1                              배포 스크립트 사본

C:\Server\MechanicalDesign_prev       직전 버전 (롤백용)
C:\Server\MechanicalDesign_venvs\
├─ backend\                           가상환경. requirements 가 바뀔 때만 재생성
└─ mcp\                               MCP 서버 가상환경 (선택 기능, 8장)
C:\Server\MechanicalDesign_data\      ← 배포가 건드리지 않는다. 백업 대상.
├─ uploads\                           업로드 이미지
├─ logs\                              service-stdout.log / service-stderr.log
└─ nssm.exe
```

**운영 데이터가 앱 폴더 바깥에 있는 것이 이 구조의 핵심이다.** 안에 있으면
코드를 통째로 갈아 끼우는 배포를 할 수 없다 — 갈아 끼울 때마다 사용자가 올린
이미지가 함께 사라진다.

가상환경도 바깥이다. 서비스가 가리키는 `python.exe` 경로가 배포마다 바뀌지
않아야 재등록이 필요 없다.

---

## 4. 서비스 관리

```powershell
Get-Service     MechanicalDesign
Restart-Service MechanicalDesign
Stop-Service    MechanicalDesign

# 로그
Get-Content C:\Server\MechanicalDesign_data\logs\service-stderr.log -Tail 50 -Wait
```

서비스가 뜨지 않아 오류를 눈으로 보고 싶으면, 서비스를 멈추고 콘솔로 띄운다.
같은 포트를 둘이 잡을 수 없으므로 **반드시 먼저 멈춘다.**

```powershell
Stop-Service MechanicalDesign
cd C:\Server\MechanicalDesign
.\run_server.ps1
```

서비스 재등록 / 제거:

```powershell
.\install-service.ps1 -AppPath 'C:\Server\MechanicalDesign'
.\install-service.ps1 -AppPath 'C:\Server\MechanicalDesign' -Uninstall
```

---

## 5. 계정 관리

로그인 없이는 화면도 API 도 열리지 않는다. 예외는 `/api/health`(기동 확인용)와
로그인·가입 화면뿐이다.

| | |
|---|---|
| 역할 | 관리자 / 일반 두 가지. 관리자만 계정을 만들고 승인·정지한다 |
| 가입 | 셀프 가입 가능. 신청은 **승인 대기** 상태로 들어오고 승인 전에는 로그인할 수 없다 |
| 로그인 유지 | refresh 쿠키 30일 (httpOnly). access 토큰은 12시간, 브라우저 메모리에만 |
| 아이디 | 이메일 형식을 강제하지 않는다. `admin` 도, `hong@사내.local` 도 된다 |
| 비밀번호 | 가입·변경 시 10자 이상. 길이 상한은 사실상 없다(한글도 안전) |

> 설치가 넣는 초기 관리자 비밀번호(`32167`)는 시드 경로라 이 10자 규칙을 거치지
> 않는다. 그래서 그 계정이 **비밀번호 정책보다 약한 채로 남는다** — 외부에서
> 닿는 서버라면 `-AdminPassword ''` 로 난수를 받거나, 로그인 후 계정 관리
> 화면에서 바꾸세요.

### 관리자가 하는 일

관리자로 로그인하면 메인 화면 오른쪽 위에 **계정 관리** 버튼이 보인다.

- **승인 대기** 탭에서 가입 신청을 승인하거나 거절한다. 거절에는 사유를 남긴다 —
  메일을 보낼 수단이 없어 나중에 관리자가 그 기록을 보고 설명해야 한다.
- **계정 만들기** 로 직접 발급하면 임시 비밀번호가 화면에 **한 번만** 표시된다.
  구두로 전달하고, 받은 사람은 첫 로그인에서 반드시 바꾸게 된다.
- **비밀번호 재설정** 도 같은 방식이다. 재설정하면 그 사람의 기존 세션이 전부 끊긴다.
- **정지** 는 즉시 효력이 있다. 이미 발급된 세션도 함께 끊는다.
- **삭제** 는 행을 지우지 않고 접근만 막는다. 그 사람이 만든 카드의 기록은 남는다.

자기 계정을 정지·삭제하거나 자기 관리자 권한을 내리는 것은 막혀 있다. 마지막
관리자도 마찬가지다 — 그러면 아무도 계정을 관리할 수 없는 상태가 되고, DB 를
직접 고치는 것 말고는 빠져나올 방법이 없다.

### 관리자 비밀번호를 잃어버렸다면

서버에서 시드 스크립트를 다시 돌린다. 계정이 이미 있으면 비밀번호를 바꾸지
않으므로, 먼저 그 계정을 지우거나 **다른** 관리자 계정을 새로 만든다.

```powershell
Stop-Service MechanicalDesign
cd C:\Server\MechanicalDesign\backend
& 'C:\Server\MechanicalDesign_venvs\backend\Scripts\python.exe' `
    scripts\seed_install.py --email 'admin2'
Start-Service MechanicalDesign
```

출력된 비밀번호로 로그인한 뒤, 계정 관리 화면에서 원래 관리자의 비밀번호를
재설정하면 된다.

---

## 6. 릴리스 만들기 (개발 쪽)

배포할 것을 정하는 행위는 **버전을 올리는 것**이다.

```powershell
# frontend/package.json 의 version 을 올린다
#   "version": "0.1.0"  →  "0.2.0"
git add frontend/package.json
git commit -m "Release v0.2.0"
git push
```

`main` 에 `frontend/package.json` 변경이 올라가면:

1. `auto-tag.yml` 이 버전을 읽어 `v0.2.0` 태그를 만든다 (이미 있으면 넘어간다).
2. 같은 실행 안에서 `release-windows.yml` 을 호출한다.
3. windows 러너가 `scripts/ci/package_deploy.ps1` 로 패키지를 만들고
   릴리스에 `deploy_package.zip` 을 붙인다.

버전을 올리지 않은 커밋은 릴리스를 만들지 않는다. `main` 에 자유롭게 커밋하면서
배포 시점만 따로 정할 수 있다.

손으로 다시 만들려면 Actions 탭에서 **Release - Windows package** 를
`workflow_dispatch` 로 실행하고 태그를 입력한다.

> 릴리스 워크플로는 **windows 러너**여야 한다. `psycopg[binary]` 의 wheel 은
> 플랫폼별이라 ubuntu 에서 만들면 서버에서 설치되지 않는다.

---

## 7. 자주 걸리는 것들

**`Python 버전 불일치` 로 멈춘다**
패키지의 wheel 은 만든 파이썬의 ABI 태그(cp313 등)를 달고 있어 다른 마이너
버전에는 설치되지 않는다. 서버에 Python 3.13 을 설치하거나, `-PythonExe` 로
올바른 python.exe 를 지정하거나, `release-windows.yml` 의 `python-version` 을
서버 버전에 맞춰 새 릴리스를 만든다.

**`폴더를 옮길 수 없습니다`**
스크립트가 무엇이 잡고 있는지 목록을 찍는다. 대개는 `<AppPath>\backend` 에
머물러 있는 터미널 창이거나, 창 없이 살아남은 `python.exe` 다. 목록이 비어
있으면 탐색기 미리 보기 창·백신 검사가 후보이고, 확실한 방법은 서버 재시작이다.
배포 중에는 어차피 앱이 멈춘다.

**`gh release download 실패`**
비공개 저장소라 인증이 없으면 받을 수 없다. `gh auth status` 로 확인한다.
운영 서버는 개발 PC 와 다른 계정으로 도는 일이 많아 인증이 따라오지 않는다.
서버에 인증을 두고 싶지 않으면 다른 PC 에서 받아 반입하고 `-ZipPath` 를 쓴다.

**포트 5176 이 이미 잡혀 있다**
`netstat -ano | findstr :5176` 로 PID 를 찾는다. 이 앱의 서비스가 이미 돌고
있는 것이라면 정상이다 — 갱신 배포는 서비스를 먼저 멈추므로 그대로 진행하면
된다.

**화면은 뜨는데 데이터가 안 나온다**
브라우저 개발자도구 Network 에서 API 요청이 어디로 가는지 본다. `localhost:5176`
으로 간다면 번들에 개발용 주소가 구워진 것이다. 패키징 스크립트가 그것을
검사해 실패시키므로 정상 릴리스에서는 나오지 않는다.

**로그인은 되는데 새로고침하면 풀린다**
`backend\.env` 의 `REFRESH_COOKIE_SECURE` 가 `true` 인데 http 로 서비스하는
경우다. 브라우저가 secure 쿠키를 http 응답에서 버리기 때문에 refresh 쿠키가
아예 저장되지 않는다. https 를 붙이지 않았다면 `false` 로 두고 서비스를
재시작한다.

**아무도 로그인할 수 없다 (계정이 없다)**
설치가 시드 단계까지 가지 못한 경우다. §5 의 "관리자 비밀번호를 잃어버렸다면"
절차로 계정을 만든다.

**마이그레이션에서 멈췄다**
스크립트가 exit 10 으로 끝난다. 새 코드는 이미 배치됐고 DB 는 일부만 적용됐을
수 있다. `rollback.ps1` 로 파일을 되돌린 뒤, DB 는 백업에서 복구할지 손으로
고칠지 판단한다.

---

## 8. MCP — 밖의 AI 가 카드를 만들게 하기 (선택)

Claude 같은 AI 가 이 시스템의 계산 카드를 읽고 만들 수 있게 하는 통로다.
**안 깔아도 앱은 그대로 돈다.** 웹에서 하던 일도 전부 그대로다.

앱과 **별개의 프로세스·별개의 가상환경**으로 돈다. `mcp` 패키지가 끌고 오는
starlette/pydantic 이 백엔드의 Flask 의존성과 부딪히기 때문이다. 섞으면 그 충돌이
배포하는 날에야 드러난다. 대신 이 서버가 죽어도 앱은 멀쩡하다.

### 8-1. 설치

의존성은 PyPI 에서 받는다(앱 배포와 다른 점 — 앱은 wheel 을 동봉한다). 선택
기능에 배포 패키지 무게를 얹지 않으려는 판단이다.

```powershell
cd C:\Server\tools\MechanicalDesign
.\install-mcp.ps1 -AppPath 'C:\Server\MechanicalDesign'

# 이 서버 안에서만 쓰려면
.\install-mcp.ps1 -AppPath 'C:\Server\MechanicalDesign' -LocalOnly
```

기본은 `0.0.0.0` 바인딩이고 **방화벽 TCP 3010 도 함께 연다**(백엔드 설치와 같은
방식). 팀원들이 각자 PC 에서 붙는 것이 이 기능의 목적이라, 열어 두지 않으면
등록 명령을 복사해 간 사람이 연결이 안 되는 이유를 한참 찾게 된다 — 주소도 맞고
서버도 떠 있는데 닿지를 않는다.

열어 두어도 되는 이유는 MCP 서버가 **자기 자격 증명을 갖지 않기** 때문이다.
들어온 토큰을 백엔드에 넘길 뿐이고, 토큰이 없거나 틀리면 백엔드가 401 로 막는다.
앱 백엔드도 이미 같은 조건으로 열려 있다.

폐쇄망이면 인터넷이 되는 PC 에서 wheel 을 모아 반입한다.

```powershell
pip wheel -r mcp_server\requirements.txt -w mcp_packages
.\install-mcp.ps1 -AppPath '...' -PackagesDir 'C:\반입\mcp_packages'
```

### 8-2. 각자 토큰을 발급받는다

**관리자 토큰 하나를 모두가 돌려 쓰지 않는다.** MCP 는 토큰 주인의 권한으로만
동작하고, 만든 카드는 그 사람이 만든 것으로 남는다. 하나를 돌려 쓰면 "누가 만든
카드인지" 가 통째로 사라진다.

1. 웹에 로그인 → 상단 **토큰** → 이름을 적고 만들기
2. **원문은 그 자리에서 한 번만 보인다.** 서버는 해시만 저장하므로 다시 볼 수
   없다
3. 화면에 **토큰과 주소가 다 채워진 명령**이 함께 뜬다. 그대로 복사해 자기
   PC 에서 실행하면 끝이다

```bash
claude mcp add --transport http mechanicaldesign http://<서버주소>:3010/mcp --header "Authorization: Bearer mdt_..."
```

주소는 앱이 알려 준다 — 사람이 이 앱에 닿은 주소면 같은 서버의 MCP 에도 닿기
때문에 평소에는 아무 설정도 필요 없다. `install-mcp.ps1 -Port` 로 포트를
바꿨거나 리버스 프록시 뒤에 두었다면 `backend\.env` 에 적는다.

```
MCP_PORT=3020
MCP_URL=https://mech.company.local/mcp    # 통째로 지정할 때
```

명령은 **한 줄**이다. 줄바꿈을 넣으면 이어 붙이는 기호가 셸마다 달라
(PowerShell 은 백틱, bash 는 역슬래시) 복사한 사람 절반이 깨진 명령을
실행하게 된다.

새거나 안 쓰게 되면 그 토큰만 폐기한다. 사람의 로그인은 그대로다.

### 8-3. MCP 로 만든 카드는 초안이다 — 사람이 게시해야 한다

**AI 가 만든 카드는 바로 모두에게 보이지 않는다.** 만든 사람과 관리자에게만
보이고, 사람이 웹에서 열어 **게시하기** 를 눌러야 공개된다.

토큰(MCP·스크립트)으로는 게시할 수 없다. 토큰은 그 사람 권한으로 돌지만, **그
사람이 숫자를 보고 눌렀는지**는 전혀 다른 얘기다. 여기를 열어 주면 AI 가 만들고
곧바로 스스로 게시하게 되어 검토 단계가 이름만 남는다.

이 구분이 필요한 이유는 검증이 보는 것이 "계산이 도는가" 이지 "공학적으로
맞는가" 가 아니기 때문이다. 단위가 어긋났거나(mm 와 m), 계수를 잘못 골랐거나,
안전율을 빠뜨려도 숫자는 멀쩡히 나온다. 그리고 그 카드로 사람이 설계 판단을 한다.

게시할 때 서버가 정의를 다시 검사한다. 없는 기호를 참조하거나 순환 참조인
카드는 게시되지 않고, 무엇이 틀렸는지 화면에 나온다.

잘못 올린 카드는 지우지 말고 **내리면** 된다(초안으로 돌아간다). 지우면 그 카드로
이미 계산해 본 사람의 근거가 통째로 사라지고, 고쳐서 다시 올릴 수도 없다.

> 사람이 웹에서 만드는 카드는 예전 그대로 바로 게시된다. 만든 사람이 화면 앞에
> 있고, 이상하면 그 자리에서 고치기 때문이다.

**카드에 흔적이 남는다.** AI 가 만든 카드에는 "AI 작성" 표시가 붙고, 사람이 만든
카드를 AI 가 고쳤으면 그 시각이 남는다. 게시하고 나면 둘 다 똑같이 생겨서,
나중에 계산이 이상하다는 얘기가 나왔을 때 어디를 먼저 볼지 알 수가 없기 때문이다.

특히 **게시한 뒤에 AI 가 또 고치면** 카드에 "게시 후 AI 수정됨" 이 붉게 뜬다.
게시한 사람이 확인한 것은 지금 내용과 다른 카드이므로, 게시 기록만 보고
검토를 거쳤다고 믿으면 안 된다. 이때는 내렸다가 다시 확인하고 올리면 된다.

### 8-4. 사용 안내는 서버가 준다

도구 선택·절차·수식 문법 같은 안내는 서버가 쥐고(`mcp_server\guide\GUIDE.md`)
`get_guide()` 도구로 내려준다. **사용자 PC 에 깔 것이 없다.**

안내를 고칠 때는 그 파일만 고친다. 호출할 때마다 읽으므로 서비스를 다시 띄울
필요도 없다.

### 8-5. Node.js 를 깔아 두는 편이 좋다

AI 가 만든 카드는 **그럴듯하게 틀린다** — 없는 기호를 참조하거나, 계산 단계를
거슬러 참조하거나, 서로를 참조해 영영 풀리지 않는다. 저장은 다 되고 오류도 안
난다.

그래서 MCP 의 `validate_card` 가 실제로 값을 넣어 계산해 본다. 그 계산기가 화면과
같은 자바스크립트 파일이라 **Node.js 가 있어야 돈다**(0장 참고). 없으면 검증이
정적 검사까지만 돌고, 그 사실이 응답의 `trial_skipped` 에 남는다.

### 8-6. 서비스 없이 손으로 띄우기

서비스가 안 뜰 때 오류를 눈으로 보거나, 관리자 권한을 못 받은 자리에서 먼저
확인해 보려면 앱 폴더의 `run_mcp_server.ps1` 을 쓴다. 백엔드의 `run_server.ps1`
과 같은 자리, 같은 방식이다.

```powershell
cd C:\Server\MechanicalDesign

# 처음이라 가상환경이 없다면 (관리자 권한이 필요 없다)
.\run_mcp_server.ps1 -Setup

# 그다음부터는
.\run_mcp_server.ps1
```

`install-mcp.ps1` 은 서비스를 등록하므로 **관리자 권한을 요구한다.** 그런데
가상환경을 만드는 일 자체는 관리자가 필요 없다 — `-Setup` 이 그 부분만 떼어
낸 것이다. 폐쇄망이면 `-PackagesDir 'C:\반입\mcp_packages'` 를 함께 준다.

가상환경 자리는 `install-mcp.ps1` 과 **같다**(`<AppPath>_venvs\mcp`). 그래서
여기서 만들어 두면 나중에 서비스로 등록할 때 그대로 쓰인다.

포트는 `backend\.env` 의 `FLASK_PORT`·`MCP_PORT` 를 읽어 맞춘다. 백엔드 주소를
숫자로 박아 두면 포트를 바꾼 설치에서 조용히 어긋나는데, 그러면 MCP 는 멀쩡히
뜨고 **도구만 전부 연결 실패로 끝나서** 원인을 찾기가 어렵다.

서비스가 이미 떠 있으면 먼저 멈춘다 — 같은 포트를 둘이 잡을 수 없다.

```powershell
Stop-Service MechanicalDesignMCP
```

### 8-7. 관리

```powershell
Get-Service     MechanicalDesignMCP
Restart-Service MechanicalDesignMCP

# 로그
Get-Content C:\Server\MechanicalDesign_data\logs\mcp-stderr.log -Tail 50

# 제거 (앱에는 영향 없음)
.\install-mcp.ps1 -AppPath 'C:\Server\MechanicalDesign' -Uninstall
```

앱을 업데이트 배포한 뒤 `mcp_server` 의 코드나 의존성이 바뀌었다면
`install-mcp.ps1` 을 한 번 더 실행한다. 같은 서비스를 지우고 다시 등록하므로
반복해서 실행해도 안전하다.

---

## 9. 백업과 복원

### 9-1. 백업

```powershell
.\backup.ps1 -AppPath 'C:\Server\MechanicalDesign' `
             -BackupRoot 'D:\backup\mechanicaldesign' -KeepDays 30
```

DB 와 업로드 이미지를 **같은 시각에 함께** 받는다. 둘 중 하나만 받으면 복구되지
않는다 — DB 에는 이미지 레코드가, uploads 에는 그 파일의 실제 내용이 있어서,
시점이 어긋나면 "DB 에는 있는데 파일이 없는" 행이 생긴다.

작업 스케줄러에 매일 걸어 둔다. `-KeepDays` 로 오래된 것은 알아서 지운다.

### 9-2. 복원 — 그리고 **리허설**

받아만 두고 복구를 해 본 적이 없는 백업은 백업이 아니다. 정작 필요한 날, 손으로
`pg_restore` 를 치면서 처음 해 보게 된다.

`restore.ps1` 의 **기본 동작이 리허설**이다. 임시 DB 에 되살려 확인하고 지운다 —
운영은 아무것도 건드리지 않는다.

```powershell
# 리허설 (안전. 월 1회쯤 돌린다)
.\restore.ps1 -AppPath 'C:\Server\MechanicalDesign' `
              -BackupDir 'D:\backup\mechanicaldesign\20260822-031500'
```

보여 주는 것:

```
  백업에 들어 있는 것:
    users                  12 행
    cards                  8 행
    variables             340 행
    calculation_records    57 행
    uploads 짝 맞음         23 개 모두 있음
리허설 통과 — 이 백업은 되살릴 수 있습니다.
```

행 수를 세어 보여 주는 이유는 **빈 백업을 받아 두고 안심하는 것**이 가장 흔한
실패이기 때문이다. 덤프는 멀쩡히 복원되는데 안에 아무것도 없다.

`uploads 짝 맞음` 은 DB 의 이미지 레코드와 실제 파일을 하나씩 맞춰 본 것이다.
한쪽만 백업되면 복원한 카드가 깨진 이미지로 뜨는데, 그때는 되돌릴 백업이 그것
하나뿐이다.

문제가 있으면 **초록불이 뜨지 않고 0 이 아닌 코드로 끝난다.** 야간 자동 점검에
걸어 두면 그 값으로 사람을 부를 수 있다.

```powershell
# 작업 스케줄러에 걸 때
.\restore.ps1 -AppPath '...' -BackupDir '...'
if ($LASTEXITCODE -ne 0) { <# 메일·알림 #> }
```

### 9-3. 진짜 복원

```powershell
.\restore.ps1 -AppPath 'C:\Server\MechanicalDesign' `
              -BackupDir 'D:\backup\mechanicaldesign\20260822-031500' -Apply
```

`-Apply` 를 붙여야만 운영을 덮어쓴다. 안전한 쪽이 기본이라야 사람이 실제로 돌려
본다.

진행 순서:

1. 리허설을 먼저 돌린다 (쓸 수 없는 백업으로 운영을 지우지 않기 위해)
2. **DB 이름을 그대로 입력**해야 진행된다. y/n 은 습관적으로 눌러진다
3. **지금 상태를 먼저 받는다** → `<AppPath>_data\pre-restore-<시각>\`
   복원은 되돌릴 수 없고, 백업을 잘못 골랐다는 것은 대개 복원한 뒤에 안다
4. 서비스를 멈추고, DB 를 **지웠다 다시 만들어** 복원한다
   (기존 위에 덮으면 백업에 없는 행이 남아 섞인다)
5. uploads 를 같은 시점의 것으로 함께 되돌린다
6. 행 수를 다시 세어 보여 주고 서비스를 올린다

복원 뒤에는 화면에서 카드와 이미지가 제대로 보이는지 확인하고, 그다음
`pre-restore-*` 폴더를 지운다.

> `-Apply` 는 사람의 입력을 받으므로 스케줄러에서는 돌지 않는다. 그게 맞다 —
> 운영을 덮어쓰는 일이 사람 없이 일어나서는 안 된다.

---

## 10. 예전 방식(git pull)에서 옮겨오기

전에는 서버에 저장소를 클론해 `setup.ps1` / `update.ps1` 로 운영했다. 그 방식은
서버에 git·Node·인터넷을 요구했고, 업데이트 중간에 깨지면 되돌릴 방법이 없었다.

옮기는 순서:

1. **백업부터 받는다.** 예전 설치의 `backend\.env` 와 `backend\uploads\` 를
   따로 복사해 둔다. DB 는 `pg_dump` 로 받는다.
2. 기존 서비스를 멈추고 제거한다.
   예전 `install-service.ps1` 이 남아 있으면 `.\install-service.ps1 -Uninstall`,
   없으면 `nssm remove MechanicalDesign confirm`.
3. **새 경로**에 `install.ps1` 로 설치한다. 기존 클론 폴더를 그대로 쓰지 않는다 —
   배포가 그 폴더를 통째로 교체하기 때문에 안에 있는 것이 모두 사라진다.
4. `<AppPath>_data\uploads` 에 예전 업로드 파일을 복사한다.
5. 예전 `.env` 의 값을 새 `.env` 에 옮긴다. **`SECRET_KEY`·`JWT_SECRET_KEY` 는
   그대로 가져와야** 기존 로그인 세션이 유지된다. `UPLOAD_DIR` 은 새로 생긴
   항목이므로 install.ps1 이 채운 값을 그대로 둔다.
6. DB 는 그대로 쓴다 — 같은 DB 를 가리키면 `flask db upgrade` 가 필요한
   마이그레이션만 추가로 적용한다. **인증이 이 단계에서 켜진다** — `users` 와
   `refresh_tokens` 테이블이 생기고, 그 순간부터 로그인 없이는 아무것도 열리지
   않는다. 기존 카드의 `created_by` 는 비어 있다(그때는 만든 사람을 알 방법이
   없었으므로 값을 지어내지 않는다).
7. 확인이 끝나면 예전 클론 폴더를 지운다.

> **사용자에게 미리 알리세요.** 이 릴리스부터 로그인 화면이 뜹니다. 설치가
> 만들어 준 관리자 계정으로 먼저 들어가 사람들의 계정을 만들어 두거나, 각자
> 가입 신청을 하게 하고 승인해 주면 됩니다(§5).

`deploy.ps1` 은 이 전환을 한 번 도와준다: `_prev` 의 `backend\uploads` 에 파일이
남아 있으면 `_data\uploads` 로 옮기고 경고를 띄운다. 안전망일 뿐이므로 5번을
직접 확인하는 편이 확실하다.

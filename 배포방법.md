# Mechanical Design 배포 방법

신규 서버에 프로젝트를 설치·실행하는 전체 절차입니다. 백엔드(Flask + Waitress)가 API와 프론트엔드 정적 파일을 **한 프로세스**로 서빙하므로 별도의 Nginx/리버스 프록시는 필요하지 않습니다.

---

## 0. 구성 요약

| 구성요소 | 역할 | 기본 포트 |
|---|---|---|
| PostgreSQL 16+ | 데이터베이스 | 5432 |
| Python 3.11+ | 백엔드 (Flask + Waitress) | 5174 |
| Node.js 18+ LTS | 프론트엔드 빌드 (배포 후엔 실행 불필요) | — |

> 브라우저는 `http://서버IP:5174` 하나로 접속 → Flask가 API와 프론트 빌드 결과를 모두 서빙.

---

## 1. 사전 소프트웨어 설치

### 1.1 PostgreSQL

**Windows**
1. <https://www.postgresql.org/download/windows/> 에서 16.x 이상 인스톨러 다운로드 후 실행
2. 설치 옵션
   - Port: `5432` (기본값)
   - Password: 기억 가능한 값 (예: 사내 표준 비밀번호)
   - Locale: `Korean, Korea` 또는 기본
3. 설치 후 `psql --version`으로 확인. 안되면 `C:\Program Files\PostgreSQL\16\bin`을 PATH에 추가

**Ubuntu/Debian**
```bash
sudo apt update
sudo apt install -y postgresql postgresql-contrib
sudo systemctl enable --now postgresql
# 관리자 비번 설정
sudo -u postgres psql -c "ALTER USER postgres WITH PASSWORD 'YOUR_PASSWORD';"
```

**RHEL/CentOS/Rocky**
```bash
sudo dnf install -y postgresql-server postgresql-contrib
sudo postgresql-setup --initdb
sudo systemctl enable --now postgresql
```

### 1.2 Python 3.11 이상

**Windows**
1. <https://www.python.org/downloads/> 에서 3.11.x 이상 다운로드
2. 인스톨러 실행 시 **"Add Python to PATH"** 반드시 체크
3. 확인
   ```cmd
   python --version
   pip --version
   ```

**Ubuntu/Debian**
```bash
sudo apt install -y python3 python3-venv python3-pip
python3 --version
```

### 1.3 Node.js 18 LTS 이상

**Windows**
1. <https://nodejs.org/> 에서 LTS 다운로드 후 설치 (기본 옵션)
2. 확인
   ```cmd
   node --version
   npm --version
   ```

**Ubuntu/Debian**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

---

## 2. 코드 배포

신규 서버에 프로젝트 폴더를 복사합니다. git clone, rsync, 압축 복사 모두 가능합니다.

```
61_MechanicalDesign/
├── backend/
│   ├── app/
│   ├── migrations/
│   ├── requirements.txt
│   ├── run.py
│   └── .env            ← 새로 작성 (다음 단계에서)
├── frontend/
│   ├── src/
│   ├── package.json
│   └── vite.config.js
└── 배포방법.md
```

> `backend/venv/`, `frontend/node_modules/`, `frontend/dist/`는 복사하지 않아도 됩니다. 각 서버에서 새로 만듭니다.

---

## 3. 데이터베이스 생성

```bash
# Windows (psql이 PATH에 있다고 가정)
psql -U postgres -c "CREATE DATABASE mechanicaldesign;"

# Linux
sudo -u postgres psql -c "CREATE DATABASE mechanicaldesign;"
```

> 기존 데이터를 옮길 필요가 없다면 여기까지만 하면 됩니다. 스키마는 `flask db upgrade`로 자동 생성됩니다.

---

## 4. 백엔드 세팅

### 4.1 가상환경 생성과 패키지 설치

```bash
cd backend

# 가상환경 생성
python -m venv venv

# 가상환경 활성화
# Windows (CMD)
venv\Scripts\activate
# Windows (PowerShell)
venv\Scripts\Activate.ps1
# Linux/Mac
source venv/bin/activate

# 패키지 설치
pip install -r requirements.txt
```

> PowerShell에서 실행 정책 오류가 나면:
> ```powershell
> Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
> ```

### 4.2 `.env` 파일 작성

`backend/.env` 파일을 새로 만들고 아래 내용을 편집합니다.

```env
# Flask
FLASK_ENV=production
FLASK_HOST=0.0.0.0
FLASK_PORT=5174
SECRET_KEY=CHANGE_ME_TO_RANDOM_STRING_1
JWT_SECRET_KEY=CHANGE_ME_TO_RANDOM_STRING_2

# Waitress (선택)
WAITRESS_THREADS=8

# Database (PostgreSQL 비밀번호는 1.1에서 설정한 값)
DATABASE_URL=postgresql+psycopg://postgres:YOUR_PASSWORD@localhost:5432/mechanicaldesign

# CORS: 같은 서버에서 서빙하면 생략 가능. 다른 도메인에서 접속할 때만 설정
CORS_ORIGINS=http://서버IP:5174
```

**필수 수정 항목**
- `SECRET_KEY`, `JWT_SECRET_KEY`: 각각 서로 다른 랜덤 문자열로 변경 (예: `python -c "import secrets; print(secrets.token_urlsafe(32))"`)
- `YOUR_PASSWORD`: PostgreSQL 관리자 비밀번호
- `CORS_ORIGINS`: 사용자가 접속할 URL

### 4.3 DB 스키마 구성

```bash
# 가상환경 활성화된 상태에서
flask db upgrade
```

모든 마이그레이션이 순서대로 적용되어 빈 스키마가 만들어집니다.

### 4.4 (선택) 업로드 폴더 권한 확인

이미지 업로드는 `backend/uploads/` 아래로 저장됩니다. 프로세스가 쓰기 권한이 있는지 확인하세요.

```bash
# Linux
mkdir -p backend/uploads
chown -R $USER:$USER backend/uploads
```

---

## 5. 프론트엔드 빌드

```bash
cd frontend
npm install
npm run build
```

빌드 결과는 `frontend/dist/`에 생성됩니다. Flask가 이 경로를 자동으로 서빙하므로 별도 파일 이동은 필요 없습니다.

### 5.1 API URL 설정 (필요한 경우만)

기본값으로 프론트는 `/api`를 사용합니다 (같은 서버 기준). 만약 백엔드를 다른 도메인이나 포트에서 서빙한다면 `frontend/.env.production`을 작성:

```env
VITE_API_URL=https://api.your-domain.com/api
```

그리고 `npm run build` 재실행.

---

## 6. 실행

### 6.1 수동 실행

```bash
cd backend
# 가상환경 활성화
venv\Scripts\activate          # Windows
source venv/bin/activate       # Linux

python run.py
```

정상 기동 시 아래와 같은 배너가 출력됩니다:

```
================================================================
|         Mechanical Design Backend API Server                 |
================================================================
|  Environment: production                                     |
|  Server:      Waitress (Production)                          |
|  Running on:  http://0.0.0.0:5174                            |
|  Threads:     8                                              |
================================================================
```

브라우저에서 `http://서버IP:5174` 접속 → 메인 페이지가 뜨면 성공.

### 6.2 systemd 서비스 등록 (Linux, 자동 시작)

`/etc/systemd/system/mechanicaldesign.service`:

```ini
[Unit]
Description=Mechanical Design Server
After=network.target postgresql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/path/to/61_MechanicalDesign/backend
EnvironmentFile=/path/to/61_MechanicalDesign/backend/.env
ExecStart=/path/to/61_MechanicalDesign/backend/venv/bin/python run.py
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now mechanicaldesign
sudo systemctl status mechanicaldesign
```

### 6.3 Windows 서비스로 등록 (NSSM 사용)

1. <https://nssm.cc/> 에서 NSSM 다운로드
2. 관리자 CMD에서:
   ```cmd
   nssm install MechanicalDesign
   ```
3. GUI에서
   - Path: `C:\...\backend\venv\Scripts\python.exe`
   - Startup directory: `C:\...\backend`
   - Arguments: `run.py`
   - Environment 탭: `FLASK_ENV=production` 등 추가
4. 서비스 시작:
   ```cmd
   nssm start MechanicalDesign
   ```

### 6.4 방화벽

```bash
# Ubuntu
sudo ufw allow 5174/tcp

# Windows
netsh advfirewall firewall add rule name="MechanicalDesign" dir=in action=allow protocol=TCP localport=5174
```

---

## 7. 동작 확인

- `http://서버IP:5174/api/health` → `{"status":"ok"}`
- `http://서버IP:5174` → 메인 카드 목록
- 카드 추가 → 설정 → 컨테이너/변수 생성 → 저장 후 계산 동작
- 이미지 업로드/표시

---

## 8. 업데이트 절차

코드 변경사항 반영:

```bash
# 1. 코드 풀/복사
git pull                          # 또는 새 버전 파일 덮어쓰기

# 2. 백엔드 의존성·스키마 업데이트
cd backend
source venv/bin/activate
pip install -r requirements.txt
flask db upgrade

# 3. 프론트 리빌드
cd ../frontend
npm install
npm run build

# 4. 백엔드 재시작
sudo systemctl restart mechanicaldesign   # Linux
nssm restart MechanicalDesign              # Windows
# 수동 실행 중이라면 Ctrl+C 후 python run.py
```

---

## 9. 자주 발생하는 문제

### "변수 구분은 input 또는 output이어야 합니다" 등 옛 에러
→ 백엔드가 구 코드로 돌고 있음. `flask db upgrade` + 백엔드 재시작.

### 프론트엔드가 흰 화면
→ `frontend/dist/`가 없거나 `backend/app/__init__.py`의 `FRONTEND_DIST_PATH`가 다른 경로를 가리킴. `npm run build` 재실행.

### API 404 (엔드포인트 정상인데)
→ `FLASK_ENV` 값 오타, 블루프린트 등록 실패, 또는 `.env`가 로드되지 않음. 서버 로그 확인.

### 이미지 업로드 후 표시 안 됨
→ `backend/uploads/` 디렉터리 쓰기 권한 확인. 업로드 경로가 DB에 저장되므로 경로를 바꾸면 기존 이미지 레코드는 404.

### `psycopg` import 오류
→ `pip install psycopg[binary]==3.2.3` 재설치. 또는 `requirements.txt` 버전 확인.

### PostgreSQL 연결 실패
- PostgreSQL 서비스 기동 여부 (`systemctl status postgresql` / Windows 서비스)
- `pg_hba.conf`에서 접속 허용 (보통 `localhost`는 기본 허용)
- `.env`의 `DATABASE_URL` 비밀번호·포트 재확인

### CORS 오류
프론트/백엔드를 같은 포트에서 서빙하면 발생하지 않음. 다른 도메인에서 접속하는 경우 `.env`의 `CORS_ORIGINS`에 해당 URL 추가 후 백엔드 재시작.

---

## 10. 보안 체크리스트

- [ ] `SECRET_KEY`, `JWT_SECRET_KEY`를 기본값이 아닌 랜덤 문자열로 변경했다
- [ ] PostgreSQL `postgres` 계정의 비밀번호를 강한 값으로 변경했다
- [ ] `.env` 파일이 git에 커밋되지 않았다
- [ ] 외부 공개 서버라면 HTTPS(Caddy 또는 Nginx reverse proxy)를 앞단에 둔다
- [ ] 방화벽에서 5174를 불필요한 외부에 노출하지 않는다
- [ ] 업로드 폴더가 공용 폴더(예: C:\Users\Public)에 있지 않다

---

## 11. 스키마 전체 초기화 (개발용)

더미 데이터를 포함한 모든 DB 내용을 버리고 싶을 때:

```bash
# 1. 백엔드 중지
# 2. DB drop & recreate
psql -U postgres -c "DROP DATABASE mechanicaldesign;"
psql -U postgres -c "CREATE DATABASE mechanicaldesign;"

# 3. 스키마 재생성
cd backend
source venv/bin/activate
flask db upgrade

# 4. 업로드 파일도 비우기
rm -rf uploads/*

# 5. 백엔드 재시작
python run.py
```

---

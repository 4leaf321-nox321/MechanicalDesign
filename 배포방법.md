# Mechanical Design 배포 방법 (Windows)

신규 서버에 프로젝트를 설치·실행하는 전체 절차입니다. 백엔드(Flask + Waitress)가 API와 프론트엔드 정적 파일을 **한 프로세스**로 서빙하므로 별도의 Nginx/리버스 프록시는 필요하지 않습니다.

대부분의 작업은 자동화 스크립트(`setup.ps1`, `update.ps1`, `install-service.ps1`)로 처리됩니다. 손으로 직접 명령어를 입력하는 옛 방식은 [부록 A](#부록-a-수동-설치-자동화-실패-시-fallback)에 별도로 남겨두었습니다.

---

## 0. 구성 요약

| 구성요소 | 역할 | 기본 포트 |
|---|---|---|
| PostgreSQL 16+ | 데이터베이스 | 5432 |
| Python 3.11+ | 백엔드 (Flask + Waitress) | 5174 |
| Node.js 18+ LTS | 프론트엔드 빌드 (빌드 후엔 실행 불필요) | — |
| NSSM | Flask 백엔드를 Windows 서비스로 등록 | — |

> 브라우저는 `http://서버IP:5174` 하나로 접속 → Flask가 API와 프론트 빌드 결과를 모두 서빙.

설치 흐름: **사전 SW 설치 → git clone → `.env` 작성 → `setup.ps1` → `install-service.ps1` → 끝.**

---

## 1. 사전 소프트웨어 설치

다음 셋이 PATH에 등록되어 있어야 합니다. PowerShell에서 `python --version`, `node --version`, `psql --version` 으로 확인.

### 1.1 PostgreSQL 16 이상

1. <https://www.postgresql.org/download/windows/> 에서 16.x 이상 인스톨러 다운로드 후 실행
2. 설치 옵션
   - Port: `5432` (기본값)
   - Password: 기억 가능한 강한 값 (운영 후 `.env`에서 사용)
   - Locale: `Korean, Korea` 또는 기본
3. 설치 후 `psql --version` 확인. 안되면 `C:\Program Files\PostgreSQL\16\bin`을 PATH에 추가하거나 `setup.ps1` 실행 시 `-PgBinDir` 인자로 경로 지정.

### 1.2 Python 3.11 이상

1. <https://www.python.org/downloads/> 에서 3.11 이상 다운로드
2. 인스톨러 실행 시 **"Add Python to PATH"** 반드시 체크
3. 확인: `python --version` 과 `pip --version`

### 1.3 Node.js 18 LTS 이상

1. <https://nodejs.org/> 에서 LTS 다운로드 후 설치 (기본 옵션)
2. 확인: `node --version` 과 `npm --version`

### 1.4 Git

이미 운영 서버에 git이 없다면 <https://git-scm.com/download/win> 에서 설치.

### 1.5 (선택) PowerShell 실행 정책

PowerShell이 처음이라면 한 번만 실행:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

## 2. 코드 가져오기

운영 서버의 적절한 위치에서 (예: `C:\apps\`):

```powershell
cd C:\apps
git clone https://github.com/4leaf321-nox321/MechanicalDesign.git
cd MechanicalDesign
```

> 운영 서버가 GitHub에 처음 접속하는 거라면 git이 자격 증명을 한 번 물어봅니다. GitHub 계정으로 로그인하거나 Personal Access Token을 입력하면 됩니다.

---

## 3. 환경 변수(.env) 작성

```powershell
copy backend\.env.example backend\.env
notepad backend\.env
```

`backend\.env`를 열어 다음 값을 채웁니다:

```env
# === Flask ===
FLASK_ENV=production            # 운영은 production 으로 (Waitress 서버 사용)
FLASK_HOST=0.0.0.0
FLASK_PORT=5174

# === Secrets (반드시 랜덤 값으로 변경) ===
SECRET_KEY=...랜덤_문자열_1...
JWT_SECRET_KEY=...랜덤_문자열_2...

# === Waitress 스레드 ===
WAITRESS_THREADS=8

# === Database ===
DATABASE_URL=postgresql+psycopg://postgres:YOUR_POSTGRES_PASSWORD@localhost:5432/mechanicaldesign

# === CORS (같은 포트에서 서빙하면 비워둠) ===
CORS_ORIGINS=
```

**필수**:
- `SECRET_KEY`, `JWT_SECRET_KEY` 는 서로 다른 랜덤 문자열로 변경:
  ```powershell
  python -c "import secrets; print(secrets.token_urlsafe(32))"
  ```
  두 번 실행해서 두 개의 키 생성.
- `DATABASE_URL` 의 `YOUR_POSTGRES_PASSWORD` 를 1.1에서 설정한 PostgreSQL 관리자 비번으로 교체.

저장 후 닫기.

---

## 4. 자동 설치 (`setup.ps1`)

PowerShell(일반 권한)에서:

```powershell
.\setup.ps1
```

이 스크립트가 자동으로 처리:

1. PostgreSQL DB 생성 (`mechanicaldesign`) — postgres 비번 한 번 물어봄
2. 백엔드 `venv` 생성, `pip install -r requirements.txt`
3. `flask db upgrade` — 모든 마이그레이션 적용 (현재 17개)
4. 프론트엔드 `npm install` + `npm run build` — `frontend\dist\` 생성

소요 시간: 보통 2~5분 (네트워크와 디스크 속도에 따라).

### 자주 발생하는 옵션

```powershell
# psql이 PATH에 없을 때 (PostgreSQL bin 경로 직접 지정)
.\setup.ps1 -PgBinDir "C:\Program Files\PostgreSQL\16\bin"

# DB는 이미 만들어둔 경우 (재설치 등)
.\setup.ps1 -SkipDbCreate
```

성공하면 다음과 같이 끝납니다:

```
============================================================
  Setup 완료
============================================================
```

---

## 5. Windows 서비스 등록 (`install-service.ps1`)

**관리자 권한 PowerShell**에서:

```powershell
.\install-service.ps1
```

이 스크립트가 자동으로 처리:

1. NSSM이 없으면 <https://nssm.cc/> 에서 자동 다운로드
2. `MechanicalDesign` 이름으로 Windows 서비스 등록
3. 부팅 시 자동 시작, 크래시 시 자동 재시작 설정
4. 로그 회전 설정 (10MB 마다)
5. 서비스 즉시 시작

성공 후 확인:

```powershell
Get-Service MechanicalDesign
# Status   Name               DisplayName
# ------   ----               -----------
# Running  MechanicalDesign   Mechanical Design Server
```

브라우저에서 `http://서버IP:5174` 접속 → 메인 화면이 뜨면 성공.

### 옵션

```powershell
# 다른 이름으로 등록
.\install-service.ps1 -ServiceName "MyMechD"

# 미리 받아둔 nssm.exe 사용
.\install-service.ps1 -NssmPath "C:\tools\nssm.exe"

# 서비스 제거
.\install-service.ps1 -Uninstall
```

### 서비스 관리 명령

```powershell
Start-Service   MechanicalDesign
Stop-Service    MechanicalDesign
Restart-Service MechanicalDesign
Get-Service     MechanicalDesign
```

### 서비스 로그

```
backend\service-stdout.log
backend\service-stderr.log
```

10MB 마다 자동 회전됩니다.

---

## 6. 방화벽 설정

외부에서 접속하려면 5174 포트를 열어야 합니다. 관리자 PowerShell:

```powershell
netsh advfirewall firewall add rule name="MechanicalDesign" dir=in action=allow protocol=TCP localport=5174
```

제거하려면:

```powershell
netsh advfirewall firewall delete rule name="MechanicalDesign"
```

---

## 7. 동작 확인

브라우저에서:

- `http://서버IP:5174/api/health` → `{"status":"ok"}`
- `http://서버IP:5174` → 메인 카드 목록
- 카드 추가 → 설정 → 컨테이너/변수 생성 → 저장 후 계산 동작
- 이미지 업로드/표시

문제가 있다면 [9. 자주 발생하는 문제](#9-자주-발생하는-문제) 참고.

---

## 8. 업데이트 절차 (`update.ps1`)

코드가 GitHub에 새로 푸시되었을 때 운영 서버에서:

```powershell
cd C:\apps\MechanicalDesign
.\update.ps1
```

이 스크립트가 자동으로 처리:

1. `git pull --ff-only` — 새 커밋 받기 (충돌 없으면)
2. `pip install -r requirements.txt` — 의존성 갱신
3. `flask db upgrade` — 새 마이그레이션 적용
4. `npm ci` + `npm run build` — 프론트 리빌드
5. `Restart-Service MechanicalDesign` — 서비스 재시작

소요 시간: 보통 1~3분.

### 옵션

```powershell
# 다른 서비스 이름
.\update.ps1 -ServiceName "MyMechD"

# 자동 재시작 안 함 (수동 실행 중일 때)
.\update.ps1 -NoRestart
```

### 운영 서버에서 직접 코드를 수정한 경우

`git pull --ff-only` 가 실패합니다. 의도된 안전장치 — 운영 서버는 GitHub의 main 브랜치만 따라가야 합니다. 수정사항이 있다면:
- 일회성이면 `git stash` 후 update 진행, 후 `git stash pop`
- 항구적이면 로컬 변경을 main에 PR로 올린 뒤 다시 update

---

## 9. 자주 발생하는 문제

### "스크립트가 차단되었습니다 / 실행할 수 없습니다"
PowerShell 실행 정책. 한 번만:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### "psql.exe를 찾을 수 없습니다"
PostgreSQL bin 경로가 PATH에 없음.
```powershell
.\setup.ps1 -PgBinDir "C:\Program Files\PostgreSQL\16\bin"
```
또는 시스템 환경변수에 추가.

### "관리자 권한 PowerShell에서 실행해야 합니다"
`install-service.ps1` 은 서비스 등록 때문에 반드시 관리자 권한 필요. 시작 메뉴에서 PowerShell 우클릭 → "관리자 권한으로 실행".

### 서비스가 Running 상태가 안 됨
`backend\service-stderr.log` 확인. 흔한 원인:
- `.env` 파일 누락 또는 오타
- PostgreSQL 미기동 (`Get-Service postgresql*` 로 확인)
- `DATABASE_URL` 비밀번호 오류 → `psycopg.OperationalError: password authentication failed`
- 5174 포트가 이미 다른 프로세스에 의해 점유됨 → `netstat -ano | findstr :5174`

### "변수 구분은 input 또는 output이어야 합니다" 등 옛 에러
백엔드가 구 코드로 돌고 있음. `update.ps1` 다시 실행. (마이그레이션 + 재시작 자동)

### 프론트엔드가 흰 화면
`frontend\dist\` 가 없거나 `backend\app\__init__.py` 의 `FRONTEND_DIST_PATH` 가 다른 경로를 가리킴.
```powershell
cd frontend
npm run build
```

### API 404 (엔드포인트 정상인데)
- `FLASK_ENV` 값 오타
- `.env` 가 로드되지 않음 → 서비스 stderr 로그에 `dev-secret-key` 등 기본값이 보이면 `.env` 미로드
- 블루프린트 등록 실패 → 서비스 stderr 로그 확인

### 이미지 업로드 후 표시 안 됨
- `backend\uploads\` 디렉터리 쓰기 권한 확인 (서비스 실행 계정 기준)
- 업로드 경로가 DB에 저장되므로 경로를 바꾸면 기존 레코드는 404

### `psycopg` import 오류
```powershell
backend\venv\Scripts\pip.exe install psycopg[binary]==3.2.3 --force-reinstall
```

### PostgreSQL 연결 실패
- 서비스 기동 여부: `Get-Service postgresql*`
- `pg_hba.conf` 에서 localhost 접속 허용 (기본값은 OK)
- `.env` 의 `DATABASE_URL` 비밀번호·포트 재확인

### CORS 오류
프론트/백엔드를 같은 포트에서 서빙하면 발생하지 않음. 다른 도메인에서 접속하는 경우 `.env` 의 `CORS_ORIGINS` 에 해당 URL 추가 후 `update.ps1` 또는 `Restart-Service`.

### `update.ps1` 의 git pull 이 비-fast-forward 거부
운영 서버에 로컬 커밋이나 수정이 있을 때 발생. [8. 업데이트 절차](#8-업데이트-절차-updateps1) 끝 부분 참고.

---

## 10. 보안 체크리스트

- [ ] `SECRET_KEY`, `JWT_SECRET_KEY` 를 기본값이 아닌 랜덤 문자열로 변경했다
- [ ] PostgreSQL `postgres` 계정의 비밀번호를 강한 값으로 변경했다
- [ ] `.env` 파일이 git에 커밋되지 않았다 (`.gitignore` 로 제외됨 — 확인용)
- [ ] 외부 공개 서버라면 HTTPS(Caddy 또는 Nginx reverse proxy)를 앞단에 둔다
- [ ] 방화벽에서 5174를 불필요한 외부에 노출하지 않는다 (내부망만 허용 등)
- [ ] 업로드 폴더가 공용 폴더(예: C:\Users\Public)에 있지 않다
- [ ] Windows 서비스 실행 계정이 최소 권한이다 (필요하면 NSSM에서 `LocalSystem` 대신 전용 계정 사용)

---

## 11. 스키마 전체 초기화 (개발용)

더미 데이터를 포함한 모든 DB 내용을 버리고 싶을 때:

```powershell
# 1. 서비스 중지
Stop-Service MechanicalDesign

# 2. DB drop & recreate (postgres 비번 입력)
psql -U postgres -c "DROP DATABASE mechanicaldesign;"
psql -U postgres -c "CREATE DATABASE mechanicaldesign;"

# 3. 스키마 재생성
cd backend
venv\Scripts\Activate.ps1
flask db upgrade
deactivate

# 4. 업로드 파일도 비우기 (선택)
Remove-Item backend\uploads\* -Recurse -Force -ErrorAction SilentlyContinue

# 5. 서비스 시작
Start-Service MechanicalDesign
```

---

## 부록 A. 수동 설치 (자동화 실패 시 fallback)

`setup.ps1` 이 어떤 이유로 동작하지 않거나 단계별 디버깅이 필요할 때 참고.

### A.1 PostgreSQL DB 생성

```powershell
psql -U postgres -c "CREATE DATABASE mechanicaldesign;"
```

### A.2 백엔드 venv + 의존성

```powershell
cd backend
python -m venv venv
venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install -r requirements.txt
```

### A.3 마이그레이션

```powershell
$env:FLASK_APP = "run.py"
flask db upgrade
```

### A.4 프론트엔드 빌드

```powershell
cd ..\frontend
npm ci
npm run build
```

### A.5 백엔드 수동 실행 (서비스 없이)

```powershell
cd ..\backend
venv\Scripts\Activate.ps1
python run.py
```

정상 기동 시 아래와 같은 배너:

```
================================================================
|         Mechanical Design Backend API Server                 |
================================================================
|  Environment: production                                     |
|  Server:      Waitress (Production)                          |
|  Running on:  http://0.0.0.0:5174                            |
|  Threads:     8                                              |
================================================================
```

Ctrl+C로 종료.

### A.6 NSSM 수동 등록

1. <https://nssm.cc/release/nssm-2.24.zip> 다운로드, `win64\nssm.exe` 추출
2. 관리자 CMD:
   ```cmd
   nssm install MechanicalDesign
   ```
3. GUI에서:
   - **Application** 탭
     - Path: `C:\apps\MechanicalDesign\backend\venv\Scripts\python.exe`
     - Startup directory: `C:\apps\MechanicalDesign\backend`
     - Arguments: `run.py`
   - **I/O** 탭 (선택)
     - Output (stdout): `C:\apps\MechanicalDesign\backend\service-stdout.log`
     - Error (stderr): `C:\apps\MechanicalDesign\backend\service-stderr.log`
4. 시작:
   ```powershell
   Start-Service MechanicalDesign
   ```

---

## 부록 B. 스크립트 비교표

| 작업 | 자동 | 수동 |
|---|---|---|
| 최초 설치 | `.\setup.ps1` | 부록 A.1 ~ A.4 |
| 서비스 등록 | `.\install-service.ps1` | 부록 A.6 |
| 업데이트 | `.\update.ps1` | `git pull` + 부록 A.2(생략) + A.3 + A.4 + `Restart-Service` |
| 서비스 제거 | `.\install-service.ps1 -Uninstall` | `nssm remove MechanicalDesign confirm` |

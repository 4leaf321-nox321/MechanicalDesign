- 변수 정의에서 테이블 데이터 행추가, 열추가 버튼을 테이블 상부에 배치
- 위젯 배치 컨테이너 많으면 아래쪽 컨테이너 변수 배치 안됨
- 변수 정의에서 변수 편집해서 기호 바꾸면, 중간값, 결과값에 사용된 변수도 같이 변경되면 좋음
- 변수 정의에서 변수타입 드롭다운의 옵션리스트를 엑셀 복사 붙이기 가능하면 좋음
- 변수 정의에서 변수 순서 변경 가능하면 좋음
- 변수 정의에서 테이블 데이터를 미리 정의해놓은 테이블을 참조해서 불러오는 기능 있으면 좋음
- 변수 정의에서 테이블 조회에 조회 행 추가하고, 행, 열, 행열 선택해서 조회하는 기능 필요
- 수식에서 문자열 합하기 가능한 기호 필요 
ex: "text" + "sample" = "textsample"